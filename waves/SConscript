#! /usr/bin/env python

# Inherit the parent construction environment
Import('env')

pytest_node = env.Command(
    chdir='waves',
    target='test_results.xml',
    source=['pytest.ini', 'conftest.py'],
    action='pytest')

env.Alias('pytest', pytest_node)
