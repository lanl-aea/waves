# TODO: find a more robust method to link to the README.txt target without assuming relative paths
Command(target="docs/README.txt",
        source="README.rst",
        action=Copy("$TARGET", "$SOURCE"))
